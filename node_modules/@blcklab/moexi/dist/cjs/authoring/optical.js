"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.DEFAULT_OPTICAL_VARIANTS = exports.DEFAULT_OPTICAL_SIZES = void 0;
exports.analyzeIconOptics = analyzeIconOptics;
exports.analyzeCollectionOptics = analyzeCollectionOptics;
const resolve_icon_js_1 = require("../core/resolve-icon.js");
exports.DEFAULT_OPTICAL_SIZES = Object.freeze([16, 20, 24]);
exports.DEFAULT_OPTICAL_VARIANTS = Object.freeze(['thin', 'regular', 'bold']);
const MINIMUM_STROKE_PX = Object.freeze({
    thin: 0.65,
    regular: 0.95,
    bold: 1.25,
});
function parseViewBox(viewBox) {
    const values = viewBox.trim().split(/[\s,]+/).map(Number);
    if (values.length !== 4 || values.some((value) => !Number.isFinite(value))) {
        throw new Error(`Cannot perform optical checks with invalid viewBox "${viewBox}".`);
    }
    return values;
}
/**
 * Performs renderer-independent scale checks for the common Moexi UI sizes.
 * This is intentionally deterministic: it verifies the viewBox/stroke contract,
 * while visual review remains the job of the generated contact sheet.
 */
function analyzeIconOptics(definition, sizes = exports.DEFAULT_OPTICAL_SIZES, variants = exports.DEFAULT_OPTICAL_VARIANTS) {
    const viewBox = definition.viewBox ?? '0 0 24 24';
    const [, , viewBoxWidth, viewBoxHeight] = parseViewBox(viewBox);
    const warnings = [];
    const samples = [];
    const squareViewBox = viewBoxWidth === viewBoxHeight;
    if (!squareViewBox)
        warnings.push('Official Moexi icons should use a square viewBox.');
    if (viewBoxWidth !== 24 || viewBoxHeight !== 24) {
        warnings.push('Official Moexi icons should use the canonical 24×24 viewBox.');
    }
    const availableVariants = new Set(Object.keys(definition.variants ?? {}));
    for (const variant of variants) {
        if (!availableVariants.has(variant)) {
            warnings.push(`Missing shared outline variant "${variant}".`);
            continue;
        }
        for (const size of sizes) {
            if (!Number.isFinite(size) || size <= 0) {
                warnings.push(`Invalid optical target size "${size}".`);
                continue;
            }
            const resolved = (0, resolve_icon_js_1.resolveIcon)(definition, { variant, size });
            const rawStroke = resolved.attrs['stroke-width'];
            const strokeWidth = typeof rawStroke === 'number' ? rawStroke : Number(rawStroke);
            const scale = size / viewBoxWidth;
            const strokePx = strokeWidth * scale;
            const minimumStrokePx = MINIMUM_STROKE_PX[variant] ?? 0.65;
            const passesMinimumStroke = Number.isFinite(strokePx) && strokePx >= minimumStrokePx;
            samples.push(Object.freeze({
                size,
                variant,
                scale,
                strokeWidth,
                strokePx,
                minimumStrokePx,
                passesMinimumStroke,
            }));
        }
    }
    const valid = squareViewBox &&
        viewBoxWidth === 24 &&
        viewBoxHeight === 24 &&
        samples.length === sizes.length * variants.length &&
        samples.every((sample) => sample.passesMinimumStroke);
    return Object.freeze({
        name: definition.name,
        valid,
        viewBox,
        viewBoxWidth,
        viewBoxHeight,
        squareViewBox,
        samples: Object.freeze(samples),
        warnings: Object.freeze(warnings),
    });
}
function analyzeCollectionOptics(definitions, sizes = exports.DEFAULT_OPTICAL_SIZES, variants = exports.DEFAULT_OPTICAL_VARIANTS) {
    const reports = definitions.map((definition) => analyzeIconOptics(definition, sizes, variants));
    const failures = reports.filter((report) => !report.valid);
    return Object.freeze({
        valid: failures.length === 0,
        sizes: Object.freeze([...sizes]),
        reports: Object.freeze(reports),
        failures: Object.freeze(failures),
    });
}
