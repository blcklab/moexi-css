"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createIconCatalog = createIconCatalog;
const capabilities_js_1 = require("../core/capabilities.js");
const validation_js_1 = require("./validation.js");
function countNodes(nodes) {
    return nodes.reduce((total, node) => total + 1 + countNodes(node.children ?? []), 0);
}
function createIconCatalog(definitions) {
    var _a;
    const validation = (0, validation_js_1.validateIconCollection)(definitions);
    if (!validation.valid) {
        const details = validation.errors.map((issue) => `${issue.path}: ${issue.message}`).join('\n');
        throw new Error(`Cannot create a catalog from invalid Moexi icons:\n${details}`);
    }
    const entries = definitions
        .map((definition) => ({
        name: definition.name,
        ...(definition.metadata?.category ? { category: definition.metadata.category } : {}),
        tags: Object.freeze([...(definition.metadata?.tags ?? [])]),
        aliases: Object.freeze([...(definition.metadata?.aliases ?? [])]),
        variants: Object.freeze(Object.keys(definition.variants ?? {}).sort()),
        ...(definition.defaultVariant ? { defaultVariant: definition.defaultVariant } : {}),
        viewBox: definition.viewBox ?? '0 0 24 24',
        nodeCount: countNodes(definition.nodes),
        ...(definition.metadata?.added ? { added: definition.metadata.added } : {}),
        ...(definition.metadata?.deprecated ? { deprecated: Object.freeze({ ...definition.metadata.deprecated }) } : {}),
        capabilities: (0, capabilities_js_1.getIconCapabilities)(definition),
    }))
        .sort((a, b) => a.name.localeCompare(b.name));
    const categories = {};
    const aliases = {};
    const deprecated = {};
    for (const entry of entries) {
        if (entry.category) {
            (categories[_a = entry.category] ?? (categories[_a] = [])).push(entry.name);
        }
        for (const alias of entry.aliases) {
            if (!(alias in aliases))
                aliases[alias] = entry.name;
        }
        if (entry.deprecated)
            deprecated[entry.name] = entry.deprecated;
    }
    const frozenCategories = Object.fromEntries(Object.entries(categories)
        .sort(([a], [b]) => a.localeCompare(b))
        .map(([category, names]) => [category, Object.freeze([...names].sort())]));
    return Object.freeze({
        icons: Object.freeze(entries),
        categories: Object.freeze(frozenCategories),
        aliases: Object.freeze(aliases),
        deprecated: Object.freeze(deprecated),
    });
}
