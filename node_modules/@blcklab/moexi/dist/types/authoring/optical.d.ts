import type { IconDefinition } from '../core/types.js';
import type { IconOpticalCollectionReport, IconOpticalReport } from './types.js';
export declare const DEFAULT_OPTICAL_SIZES: readonly [16, 20, 24];
export declare const DEFAULT_OPTICAL_VARIANTS: readonly ["thin", "regular", "bold"];
/**
 * Performs renderer-independent scale checks for the common Moexi UI sizes.
 * This is intentionally deterministic: it verifies the viewBox/stroke contract,
 * while visual review remains the job of the generated contact sheet.
 */
export declare function analyzeIconOptics(definition: IconDefinition, sizes?: readonly number[], variants?: readonly string[]): IconOpticalReport;
export declare function analyzeCollectionOptics(definitions: readonly IconDefinition[], sizes?: readonly number[], variants?: readonly string[]): IconOpticalCollectionReport;
