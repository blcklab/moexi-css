import type { IconDefinition } from '../core/types.js';
/**
 * Applies conservative, semantics-preserving cleanup to an icon definition.
 * It does not simplify path data or invent geometry.
 */
export declare function optimizeIcon(definition: IconDefinition): Readonly<IconDefinition>;
