import type { IconDefinition, IconRenderOptions, IconVariantName } from '../core/types.js';
/** Returns a standalone SVG string. Safe for SSR and static HTML generation. */
export declare function renderSvg<T extends IconDefinition>(definition: T, options?: IconRenderOptions<IconVariantName<T>>): string;
