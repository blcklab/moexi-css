import type { IconCapabilities, IconDefinition } from './types.js';
/**
 * Derives renderer-neutral capabilities from the canonical definition.
 * Official icons and third-party/custom icons use exactly the same path.
 */
export declare function getIconCapabilities(definition: IconDefinition): IconCapabilities;
