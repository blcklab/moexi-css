import type { IconDefinition, IconPackDefinition, IconRegistryEntry, IconRegistryOptions, IconRegistryResolution } from './types.js';
/**
 * Runtime registry for name/alias lookup across core or third-party packs.
 * The registry never emits console warnings; deprecation information is returned
 * as data so each consumer can decide how to present it.
 */
export declare class IconRegistry {
    #private;
    constructor(options?: IconRegistryOptions);
    register(definition: IconDefinition, namespace?: string, packName?: string): this;
    registerPack(pack: IconPackDefinition): this;
    resolve(name: string): IconRegistryResolution | undefined;
    get(name: string): IconDefinition | undefined;
    has(name: string): boolean;
    keys(): readonly string[];
    entries(): readonly IconRegistryEntry[];
    get size(): number;
}
export declare function createIconRegistry(options?: IconRegistryOptions): IconRegistry;
