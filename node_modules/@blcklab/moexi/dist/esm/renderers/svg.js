import { escapeXml, normalizeAttributeName } from '../core/attributes.js';
import { resolveIcon } from '../core/resolve-icon.js';
function renderAttributes(attrs) {
    const parts = [];
    for (const [rawName, value] of Object.entries(attrs)) {
        if (value === null || value === undefined || value === false)
            continue;
        const name = normalizeAttributeName(rawName);
        const serializedValue = value === true ? '' : String(value);
        parts.push(`${name}="${escapeXml(serializedValue)}"`);
    }
    return parts.length > 0 ? ` ${parts.join(' ')}` : '';
}
function renderNode(node) {
    const attributes = renderAttributes(node.attrs ?? {});
    const children = node.children?.map(renderNode).join('') ?? '';
    if (children.length > 0) {
        return `<${node.tag}${attributes}>${children}</${node.tag}>`;
    }
    return `<${node.tag}${attributes}/>`;
}
/** Returns a standalone SVG string. Safe for SSR and static HTML generation. */
export function renderSvg(definition, options = {}) {
    const icon = resolveIcon(definition, options);
    const attributes = renderAttributes({
        xmlns: 'http://www.w3.org/2000/svg',
        viewBox: icon.viewBox,
        ...icon.attrs,
    });
    const title = icon.title ? `<title>${escapeXml(icon.title)}</title>` : '';
    const nodes = icon.nodes.map(renderNode).join('');
    return `<svg${attributes}>${title}${nodes}</svg>`;
}
