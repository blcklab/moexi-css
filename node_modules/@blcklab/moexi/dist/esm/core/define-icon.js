import { deepFreeze } from './freeze.js';
/**
 * Declares an immutable icon using the Moexi icon specification.
 * The function intentionally performs no framework-specific work.
 */
export function defineIcon(definition) {
    return deepFreeze(definition);
}
