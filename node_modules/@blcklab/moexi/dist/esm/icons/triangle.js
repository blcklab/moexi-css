import { defineIcon } from '../core/define-icon.js';
import { outlineVariants } from '../core/official-variants.js';
export const triangle = defineIcon({
    name: 'triangle',
    metadata: {
        'category': 'symbols',
        'tags': [
            'shape',
            'geometry',
            'delta'
        ],
        'added': '0.4.0'
    },
    nodes: [
        {
            'tag': 'polygon',
            'attrs': {
                'points': '12 4 21 20 3 20 12 4'
            }
        }
    ],
    defaultVariant: 'regular',
    variants: outlineVariants,
});
export default triangle;
